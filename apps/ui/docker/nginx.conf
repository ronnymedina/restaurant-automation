server {
  listen 80;
  # IPv6: requerido para ser alcanzable por la red privada de Railway (IPv6-only).
  listen [::]:80;
  server_name _;

  root /usr/share/nginx/html;

  location / {
    try_files $uri $uri/index.html /index.html;
  }
}
